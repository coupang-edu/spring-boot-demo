spring:
  datasource:
    url: jdbc:mysql://${DATABASE_HOST:127.0.0.1}:3306/demo?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  thymeleaf.cache: false
  mvc.hiddenmethod.filter.enabled: true
